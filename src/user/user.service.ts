import { Injectable } from '@nestjs/common';

// TODO: Implement UserService with the following methods:
// - findAll(query): List all users (admin only) with pagination
// - findOne(id): Get user by ID
// - findByEmail(email): Get user by email (for auth)
// - update(id, updateUserDto): Update user details
// - deactivate(id): Soft delete/deactivate user (admin only)
// - getStats(): User statistics for dashboard (admin only)

@Injectable()
export class UserService {}
